services:
  - type: web
    name: mario-server
    runtime: node
    plan: free
    buildCommand: "npm install && npm rebuild sqlite3 --build-from-source && npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
