services:
  - type: web
    name: mario-server
    runtime: node
    plan: free
    buildCommand: "cd mario-multiplayer/client && npm install && npm run build && cd ../server && npm install"
    startCommand: "cd mario-multiplayer/server && npm start"
    envVars:
      - key: NODE_ENV
        value: production
